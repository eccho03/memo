<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">
    
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#ffffff">

        <ImageView
            android:id="@+id/editText_iv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:src="@drawable/memo_img">
        </ImageView>

        <EditText
            android:id="@+id/editText_et"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignLeft="@+id/editText_iv"
            android:layout_alignTop="@+id/editText_iv"
            android:layout_alignRight="@+id/editText_iv"
            android:layout_alignBottom="@+id/editText_iv"
            android:layout_centerInParent="true"
            android:gravity="center"
            android:hint="내용을 입력하세요."
            android:textColor="@color/black">

        </EditText>
    </RelativeLayout>

    <Button
        android:id="@+id/nextScreen_btn"
        android:layout_width="200dp"
        android:layout_height="50dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginBottom="50dp"
        android:text="다음 화면"
        android:textSize="20sp">
    </Button>


</androidx.constraintlayout.widget.ConstraintLayout>